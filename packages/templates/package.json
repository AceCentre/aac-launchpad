{
  "name": "templates",
  "version": "0.0.0",
  "main": "./dist/index.js",
  "types": "./dist/index.d.ts",
  "source": "./src/index.ts",
  "private": true,
  "files": [
    "dist/**"
  ],
  "scripts": {
    "test": "echo \"No tests, this folder is just static data\"",
    "dev": "tsc -w",
    "build": "tsc && cp -r src/activity-book/*.json dist/activity-book/",
    "lint": "TIMING=1 eslint src --fix",
    "clean": "rm -rf .turbo && rm -rf node_modules && rm -rf dist"
  },
  "devDependencies": {
    "eslint": "^7.32.0",
    "scripts": "*",
    "tsconfig": "*",
    "types": "*",
    "board-to-pdf": "*",
    "typescript": "^4.5.3"
  },
  "jest": {
    "preset": "scripts/jest/node"
  }
}
